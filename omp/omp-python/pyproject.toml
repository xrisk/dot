[project]
name = "omp-python-tools"
version = "0.1.0"
description = "Python tools used by the local OMP coding harness"
requires-python = ">=3.14,<3.15"
dependencies = [
  "basedpyright",
  "debugpy",
  "python-lsp-server[all]",
  "ruff",
]

[tool.uv]
package = false
